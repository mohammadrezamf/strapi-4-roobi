'use strict';

/**
 * schedule-timing controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schedule-timing.schedule-timing');
